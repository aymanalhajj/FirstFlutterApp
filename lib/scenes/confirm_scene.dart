import 'package:flutter/widgets.dart';

import 'custom_dialogs.dart';

class ConfirmScene extends StatelessWidget {
  const ConfirmScene({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
