import 'package:flutter/widgets.dart';

class ProductDetailScene extends StatelessWidget{

  const ProductDetailScene({super.key});

  @override
  Widget build(BuildContext context){
    return Container();
  }
}